#include<stdio.h>
int fact (int n)
{
	if ( n==0 || n==1)
	{
		return n;
	}
	else 
	{
		return n*fact(n-1);
	}
}
int main()
{
	int n;
printf("enter the value of n: ");
scanf("%d",&n);

int p=fact(n);
{
	printf("the factorial = %d ",p);
}
return 0;
}
